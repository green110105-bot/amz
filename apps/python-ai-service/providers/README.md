# Provider Adapters

Mock-first adapter boundary for LLM APIs, Keepa, SellerSprite, Helium 10, Amazon SP-API, and Amazon Ads API.

All adapters must return deterministic fixtures by default and include source, confidence, and freshness metadata. Real credentials are not stored here.
